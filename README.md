# Latin-Crossword

Using Mckeown's Latin-English Dictionary to produce a json library of Latin words which are used to generate a crossword with the hints being the definitions of the words. So far a word is treated as a specific string of characters.

